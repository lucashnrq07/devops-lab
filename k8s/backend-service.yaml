apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend
  type: NodePort
  ports:
    - protocol: TCP
      port: 3001        # porta interna do cluster
      targetPort: 3001  # porta do container
      nodePort: 30011   # porta externa para acessar no localhost
